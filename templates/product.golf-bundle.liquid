  <link href="{{'normalize.css' | asset_url}}" async rel="stylesheet" type="text/css">
  <link href="{{'webflow.css' | asset_url}}" async rel="stylesheet" type="text/css">
  <link href="{{'product.golf.css' | asset_url}}" async rel="stylesheet" type="text/css">
  <link href="https://fonts.googleapis.com" rel="preconnect">
  <link href="https://fonts.gstatic.com" rel="preconnect" crossorigin="anonymous">
  <script src="https://ajax.googleapis.com/ajax/libs/webfont/1.6.26/webfont.js" type="text/javascript"></script>
  <script type="text/javascript">WebFont.load({  google: {    families: ["Montserrat:100,100italic,200,200italic,300,300italic,400,400italic,500,500italic,600,600italic,700,700italic,800,800italic,900,900italic"]  }});</script>
  <!-- [if lt IE 9]><script src="https://cdnjs.cloudflare.com/ajax/libs/html5shiv/3.7.3/html5shiv.min.js" type="text/javascript"></script><![endif] -->
  <script type="text/javascript">!function(o,c){var n=c.documentElement,t=" w-mod-";n.className+=t+"js",("ontouchstart"in o||o.DocumentTouch&&c instanceof DocumentTouch)&&(n.className+=t+"touch")}(window,document);</script>
  <link href="images/favicon.ico" rel="shortcut icon" type="image/x-icon">
  <link href="images/webclip.png" rel="apple-touch-icon">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@8/swiper-bundle.min.css">
  <style>ton-field:nth-child(1) .radio-button-label {
    color: #FBBC04;
  }
  .pack-price-difference {
    pointer-events: none;
  }
  .swiper-pagination-bullet {
    width: 8px;
    height: 8px;
    background: #fff;
    margin-left: 6px !important;
    margin-right: 6px !important;
    opacity: 0.32;
  }
  .swiper-pagination-bullet.swiper-pagination-bullet-active {
  	background: #fff;
    opacity: 1;
  }
  /* Hide scrollbar for Chrome, Safari and Opera */
  .navbar5_menu::-webkit-scrollbar {
    display: none;
  }
  /* Hide scrollbar for IE, Edge and Firefox */
  .navbar5_menu {
    -ms-overflow-style: none;  /* IE and Edge */
    scrollbar-width: none;  /* Firefox */
  }
  @media screen and (max-width: 320px) {
  	.product-header1_payment-methods {
    	column-gap: 6px;
    }
  }
  #productGalleryButtons:before, #productGalleryButtons:after{
    content: unset;
  }
</style>
{% section 'sw--product-banner' %}
{% section 'product_golf_budle' %}
{% section 'related-products' %}
{% section 'recently-viewed-products' %}
{% section 'sw--maney-back' %}
{% section 'sw--bundle-scrollbar' %}

<script type="text/javascript">
  var _learnq = _learnq || [];
  var item = {
    Name: {{ product.title|json }},
    ProductID: {{ product.id|json }},
    Categories: {{ product.collections|map:'title'|json }},
    ImageURL: "https:{{ product.featured_image.src|img_url:'grande' }}",
    URL: "{{ shop.secure_url }}{{ product.url }}",
    Brand: {{ product.vendor|json }}, 
    Price: {{ product.price|money|json }},
    CompareAtPrice: {{ product.compare_at_price_max|money|json }}
  };
  _learnq.push(['track', 'Viewed Product', item]);
  _learnq.push(['trackViewedItem', {
    Title: item.Name,
    ItemId: item.ProductID,
    Categories: item.Categories,
    ImageUrl: item.ImageURL,
    Url: item.URL,
    Metadata: {
      Brand: item.Brand,
      Price: item.Price,
      CompareAtPrice: item.CompareAtPrice
    }
  }]);
</script>
 
<script type="text/javascript">
  var _learnq = _learnq || [];
  var classname = document.getElementsByClassName("ProductForm__AddToCart Button shopify-payment-button__button shopify-payment-button__button--unbranded  Button--full");
  var addToCart = function() {
  _learnq.push(['track', 'Added to Cart', item]);
  };   for (var i = 0; i < classname.length; i++) {
  classname[i].addEventListener('click', addToCart, false);
  }
  </script>


  <script src="{{'webflow.js' | asset_url}}" async type="text/javascript"></script>
  <!-- [if lte IE 9]><script src="https://cdnjs.cloudflare.com/ajax/libs/placeholders/3.0.2/placeholders.min.js"></script><![endif] -->
  <script src="https://cdn.jsdelivr.net/npm/swiper@8/swiper-bundle.min.js"></script>
    
  <!-- Fotorama from CDNJS, 19 KB -->
  <link  href="https://cdnjs.cloudflare.com/ajax/libs/fotorama/4.6.4/fotorama.css" rel="stylesheet">
  <script src="https://cdnjs.cloudflare.com/ajax/libs/fotorama/4.6.4/fotorama.js"></script>

  <script>
  var swiperButtons = new Swiper("#socialSwiper", {
    loop: true,
    spaceBetween: 16,
    slidesPerView: 1,
    freeMode: false,
    // Enable lazy loading
    lazy: true,
    keyboard: {
      enabled: true,
    },
    navigation: {
      nextEl: '#nextArrow',
      prevEl: '#prevArrow',
    },
    pagination: {
      el: '#witnessPagination',
      type: 'bullets',
    },
    breakpoints: {
      480: {
        slidesPerView: 2,
      },
      992: {
        slidesPerView: 3,
      }
    }
  });
</script>
  <script>
  var swiperButtonsTwoo = new Swiper("#productGalleryButtons", {
    loop: false,
    spaceBetween: 8,
    slidesPerView: 5,
    freeMode: false,
    allowTouchMove: false,
    centeredSlides: false,

    lazy: true,
    keyboard: {
      enabled: true,
    },
    breakpoints: {
      640: {
        direction: 'horizontal',
      },
    }
  });
  var mySwiper = new Swiper('#productGallery', {
    // Optional parameters
    spaceBetween: 16,
    slidesPerView: 1,
    loop: true,
    autoplay: {
        delay: 5000,
        disableOnInteraction: false,
      },
    effect: 'fade',
    thumbs: {
      swiper: swiperButtonsTwoo,
    },
    pagination: {
      el: '#productPagination',
      type: 'bullets',
    }
  });

  {%- comment -%}
  function updatePrices(click__butt) {
    var $clickButton = $(click__butt);
    var prc = $clickButton.data("prc");
    var price = $clickButton.data("price");
    var oldprice = $clickButton.data("old-price");
    var pr_id = $clickButton.data("id");
    var comPrice = item.CompareAtPrice;
    var prid = $clickButton.data("id");
    var $productHeaderPrice = $(".product-header1_price_difference");
    var $cta_price_difference = $(".cta_price_difference");
    var yousave = Math.round(100-(_BISConfig.product.price/_BISConfig.product.compare_at_price*100));

    var data_quan = $clickButton.data("quan");

    console.log(yousave);
    
    $("#you_save_per").html(yousave);
    
    $("#main-price").html(price);
    $("#main_pr_id").val(pr_id);
    $("#comp-price").html(comPrice);
    $("#all_quant").val(data_quan);
    $("#ATC_price").html(price);
    $("#ATC_old_price").html(oldprice);
    $("#product-select-8340685259077").val(prid);
  }
   
  $(document).ready(function() {
    updatePrices(".single_quantity.selected");
  });
  
  $(".single_quantity").on("click", function() {
    updatePrices(this);
    
    var data_quan = $(this).data("qty");
    $("#all_quant").val(data_quan);
  });
  {%- endcomment -%}

$(document).ready(function() {
  $('.product-header1_accordion').click(function() {
    $(this).next('.accordion-content').slideToggle();
    $(this).toggleClass('active');
  });
});
</script>